{
  "name": "chords",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Search a song, get its chord chart, transpose it, read it in Nashville numbers, print it.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@tonaljs/tonal": "^4.10.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
